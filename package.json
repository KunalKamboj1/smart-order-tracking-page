{
  "name": "smart-order-tracking-page-builder",
  "version": "1.0.0",
  "description": "A Shopify app that lets merchants create branded order tracking experiences",
  "main": "server/index.js",
  "scripts": {
    "dev": "shopify app dev",
    "build": "shopify app build",
    "deploy": "shopify app deploy",
    "generate": "shopify app generate",
    "server": "node server/index.js",
    "client": "cd client && npm start",
    "install:all": "npm install && cd client && npm install && cd ../extensions/tracking-page && npm install"
  },
  "dependencies": {
    "@shopify/shopify-api": "^9.0.2",
    "@shopify/shopify-app-express": "^2.1.0",
    "@shopify/shopify-app-session-storage-sqlite": "^2.0.0",
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "helmet": "^7.0.0",
    "compression": "^1.7.4"
  },
  "devDependencies": {
    "@shopify/cli": "^3.49.0",
    "nodemon": "^3.0.1"
  },
  "author": "Smart Order Tracking Team",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  }
}